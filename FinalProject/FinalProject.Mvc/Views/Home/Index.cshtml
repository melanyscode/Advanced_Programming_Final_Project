@{
    ViewBag.Title = "Home Page";
}
@model FinalProject.Mvc.Models.DashboardView

<style>
   
    .user-card-add{
        text-align:center;
        display: flex;
        justify-content: center;
    }
    .progress-circle {
        --size: 200px;
        --progress: 0;
        --color: #00c7c7;
        width: var(--size);
        height: var(--size);
        border-radius: 50%;
        background: conic-gradient(var(--color) calc(var(--progress) * 1%), #ddd 0);
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        font-size: 30px;
        color: var(--color);
        user-select: none;
        position: relative;
        box-shadow: 0 0 8px var(--color);
        text-align: center;
    }

        .progress-circle::before {
            content: '';
            position: absolute;
            width: calc(var(--size) - 30px);
            height: calc(var(--size) - 30px);
            background: #1e1e2f;
            border-radius: 50%;
            z-index: 1;
        }

        .progress-circle > span {
            position: relative;
            z-index: 2;
            color: white;
        }

    form.remove-task-form {
        position: absolute;
        top: 8px;
        right: 8px;
        margin: 0;
    }

        form.remove-task-form button {
            background-color: transparent;
            color: white;
            border: none;
            font-size: 22px;
            cursor: pointer;
            line-height: 1;
        }

    .task-name {
        margin-top: 15px;
        font-size: 18px;
        font-weight: 600;
        text-align: center;
        word-break: break-word;
    }
</style>

<main>


    <section class="row" aria-labelledby="aspnetTitle">
        <h1 id="title">AKI DAAAAAAAAASHBOARD</h1>

        <form method="post" action="@Url.Action("AddTask")" style="margin-bottom: 20px;">
            <select name="taskId" class="switch-btn" required>
                <option value="">Select Tasks</option>
                @foreach (var task in Model.AllTasks)
                {
                    <option value="@task.TaskId">@task.TaskName</option>
                }
            </select>
            <select name="chart" class="switch-btn " required>

                <option value="1">Select Chart Type</option>
                <option value="1">Donut Chart</option>
                <option value="2">Line Chart</option>
                <option value="3">Bar Chart</option>

            </select>
            <select name="interval" class="switch-btn " required>

                <option value="24">Select Interval</option>
                <option value="1">Every hour</option>
                <option value="2">Every 2 hours</option>
                <option value="3">Every three hours</option>
                <option value="5">Every 5 hours</option>
                <option value="8">Every 8 hours</option>
                <option value="24">Every 24 Hours</option>
            </select>
            <button type="submit" class="switch-btn active">Add</button>
        </form>

        <h2 class="text-center">Monitoring Dashboard</h2>


        <div id="task-cards-container" style="display:flex; flex-wrap: wrap; gap: 20px;">

        </div>

    </section>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        async function fetchUserTasks() {
            try {
                const response = await fetch('@Url.Action("GetUserTasks")');
                const tasks = await response.json();

                const container = document.getElementById('task-cards-container');
                container.innerHTML = '';

               tasks.forEach(task => {
                    let val = parseInt(task.Result?.replace("%", "").trim()) || 0;
                    val = Math.min(100, Math.max(0, val));

                    const taskCard = document.createElement('div');
                    taskCard.className = 'user-card';

                    const form = document.createElement('form');
                    form.method = 'post';
                    form.action = '@Url.Action("RemoveTask")';
                    form.className = 'remove-task-form';


                    taskCard.appendChild(form);

                   if (task.ChartType === 1) {
                        const form = document.createElement('form');
                        form.method = 'post';
                        form.action = '@Url.Action("RemoveTask")';
                        form.className = 'remove-task-form';

                        const hiddenInput = document.createElement('input');
                        hiddenInput.type = 'hidden';
                        hiddenInput.name = 'taskId';
                        hiddenInput.value = task.TaskId;
                        form.appendChild(hiddenInput);

                        const btn = document.createElement('button');
                        btn.type = 'submit';
                        btn.setAttribute('aria-label', 'Remove task');
                        btn.innerHTML = '&times;';
                        form.appendChild(btn);

                        taskCard.appendChild(form);
                        const graphic = document.createElement('div');
                        graphic.className = "user-card-add";

                        const progressCircle = document.createElement('div');
                        progressCircle.className = 'progress-circle';
                        progressCircle.style.setProperty('--progress', val);
                        progressCircle.innerHTML = `<span>${val}%</span>`;
                        graphic.appendChild(progressCircle);

                        taskCard.appendChild(graphic);

                   } else if (task.ChartType === 2) {
                        const form = document.createElement('form');
                        form.method = 'post';
                        form.action = '@Url.Action("RemoveTask")';
                        form.className = 'remove-task-form';

                        const hiddenInput = document.createElement('input');
                        hiddenInput.type = 'hidden';
                        hiddenInput.name = 'taskId';
                        hiddenInput.value = task.TaskId;
                        form.appendChild(hiddenInput);

                        const btn = document.createElement('button');
                        btn.type = 'submit';
                        btn.setAttribute('aria-label', 'Remove task');
                        btn.innerHTML = '&times;';
                        form.appendChild(btn);

                        taskCard.appendChild(form);
                        console.log(task.history);

                        const canvas = document.createElement('canvas');
                        canvas.style.width = "200px";
                        canvas.style.height = "150px";
                        taskCard.appendChild(canvas);

                        const labels = task.History.map(h => {
                            const date = new Date(h.date);
                            console.log( "fecha " + h.date)
                            return date.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
                        });

                        console.log(typeof labels)
                        const dataValues = task.History.map(h => h.value);

                        new Chart(canvas.getContext('2d'), {
                            type: 'line',
                            data: {
                                labels: labels,
                                datasets: [{
                                    label: task.TaskName,
                                    data: dataValues,
                                    borderColor: '#00c7c7',
                                    backgroundColor: 'rgba(0, 199, 199, 0.2)',
                                    fill: true,
                                    tension: 0.3
                                }]
                            },
                            options: {
                                responsive: false,
                                scales: {
                                    y: {
                                        beginAtZero: true,
                                        max: 100
                                    }
                                }
                            }
                        });

                   } else if (task.ChartType === 3) {
                        const form = document.createElement('form');
                        form.method = 'post';
                        form.action = '@Url.Action("RemoveTask")';
                        form.className = 'remove-task-form';

                        const hiddenInput = document.createElement('input');
                        hiddenInput.type = 'hidden';
                        hiddenInput.name = 'taskId';
                        hiddenInput.value = task.TaskId;
                        form.appendChild(hiddenInput);

                        const btn = document.createElement('button');
                        btn.type = 'submit';
                        btn.setAttribute('aria-label', 'Remove task');
                        btn.innerHTML = '&times;';
                        form.appendChild(btn);

                        taskCard.appendChild(form);
                        console.log(task.history);

                        const canvas = document.createElement('canvas');
                        canvas.style.width = "200px";
                        canvas.style.height = "150px";
                        taskCard.appendChild(canvas);

                        const labels = task.History.map(h => {
                            const date = new Date(h.date);
                            console.log("fecha " + h.date)
                            return date.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
                        });

                        console.log(typeof labels)
                        const dataValues = task.History.map(h => h.value);

                        new Chart(canvas.getContext('2d'), {
                            type: 'bar',
                            data: {
                                labels: labels,
                                datasets: [{
                                    label: task.TaskName,
                                    data: dataValues,
                                    borderColor: '#00c7c7',
                                    backgroundColor: 'rgba(0, 199, 199, 0.2)',
                                    fill: true,
                                    tension: 0.3
                                }]
                            },
                            options: {
                                responsive: false,
                                scales: {
                                    y: {
                                        beginAtZero: true,
                                        max: 100
                                    }
                                }
                            }
                        });
                    }


                    const taskNameDiv = document.createElement('div');
                    taskNameDiv.className = 'task-name';
                    taskNameDiv.textContent = task.TaskName;
                    taskCard.appendChild(taskNameDiv);

                    container.appendChild(taskCard);
                });


            } catch (error) {
                console.error('Error fetching tasks:', error);
            }
        }

        setInterval(fetchUserTasks, 5000);


        fetchUserTasks();
    </script>

</main>
